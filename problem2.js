
// Task
// Take four parameters. Multiply the four numbers and then return the result

function fourNumber(a,b,c,d) {
          const sum = a + b + c + d;
          const result = sum;
          return result;
}
const finalResult = fourNumber(1500,2500,2500,3500);
console.log(finalResult);
